#This is the requirements file for the Solana module of the Rosetta project. This file specifies the necessary 
#Python packages and their VERSION needed to work with the Solana blockchain and the Anchor framework.

# Note: Please ensure that you have the correct versions of the packages installed to avoid compatibility issues.

# You can use pip install -r requirements.txt to install all the required packages.

# -------------------- BLOCKCHAIN --------------------
# Solana & Anchor Framework
anchorpy==0.18.0
anchorpy-core==0.1.3
solana==0.30.2
cargo 1.86.0 (4d1f5b1f6 2024-05-16)
solders==0.18.1
base58==2.1.1
based58==0.1.1

# Async Support && webapp
nest-asyncio==1.6.0
streamlit==1.50.0
Flask==3.1.2
toml==0.10.2
